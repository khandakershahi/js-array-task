/**
 * ### 5. Combining Arrays

**Instructions:**

1. Create two arrays of your choice.
2. Use the concat method to combine the two arrays into a new array.
3. Print both the original arrays and the combined array using console.log().

 */

const arrayFirst = ['HTML5', 'CSS3', 'Javascript', 'React', 'MongoDB'];

const arraySecond = ['Human', 'Animal', 'Bird', 'Ant'];

const joinArry = arrayFirst.concat(arraySecond);


console.log(arrayFirst);

console.log(arraySecond);

console.log(joinArry);